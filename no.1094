#include <stdio.h>

int main(void)
{
    int number;
    scanf("%d", &number);
    
    int students[number];
    
    for (int i = 0; i < number; i++)
    {
        scanf("%d", &students[i]);
    }
    
    for (int i = number - 1 ; i >= 0; i--)
    {
        printf("%d ", students[i]);
    }
}
