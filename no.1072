#include <stdio.h>

int main(void)
{
    int count, number;
    
    scanf("%d", &count);
    
    while (count != 0)
    {
        scanf("%d", &number);
        printf("%d\n", number);
        count--;
    }
}
