#include <stdio.h>

int main(void)
{
    char c;
    
    while (1)
    {
        scanf("%c\r", &c);
        printf("%c\n", c);
        
        if (c == 'q')
        {
            break;
        }
    }
}
