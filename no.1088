#include <stdio.h>

int main(void)
{
    int number;
    
    scanf("%d", &number);
    
    for (int i = 1; i <=number; i++)
    {
        if (i % 3 == 0)
        {
            continue;
        }
        
        printf("%d ", i);
    }
}
