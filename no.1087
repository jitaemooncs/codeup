#include <stdio.h>

int main(void)
{
    int number;
    
    long long sum = 0;
    
    scanf("%d", &number);
    
    for (int i = 1; i <= number; i++)
    {
        sum += i;
        
        if (sum >= number)
        {
            break;
        }
    }
    
    printf("%d\n", sum);
}
