#include <stdio.h>
#include <string.h>

int main(void)
{
    // Getting word less than 20 letters
    char word[21];
    
    scanf("%s", word);
    
    for (int i = 0, n = strlen(word); i < n ; i++)
    {
        printf("%c\n", word[i]);
    }
}
